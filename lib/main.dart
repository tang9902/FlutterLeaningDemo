import 'package:flutter/material.dart';

import 'Widgets/c_HandAccountDemo/HandAccountDemo.dart';


void main() {
  // WidgetFlutterBinding是用来与引擎交互的，确保它的实例是存在的，是保证运行的关键
  WidgetsFlutterBinding.ensureInitialized();
  // runApp(ProviderShopperDemo());
  // runApp(TestFinalDemo());
  // runApp(TestRebuild());
  // runApp(MyApp());
  // runApp(BasicWidgetsDemo());
  // runApp(MaterialComponentsDemo());
  // runApp(ResponseToPutDemo());
  // runApp(StateFlowDirectionDemo());
  // runApp(ShoppingListDemo());
  // runApp(TestDidUpdateWidget());
  // runApp(GridViewDemo());
  // runApp(StackDemo());
  // runApp(CardDemo());
  // runApp(ConstraintDemo());
  // runApp(NavigatorDemo());
  // runApp(NavigatorNamedRouteDemo());
  // runApp(OnGenerateRouteDemo());
  // runApp(Navigator2Demo());
  // runApp(Navigator21Demo());
  // runApp(Navigator22Demo());
  // runApp(AppStateDemo());
  // runApp(TestInheritedWidgetRebuild());
  // runApp(ProviderDemo());
  // runApp(ChangeNotifierProviderDemo());
  // runApp(FutureProviderDemo());
  // runApp(StreamProviderDemo());
  // runApp(ValueListenableProviderDemo());
  // runApp(ProxyProviderDemo());
  // runApp(SQLiteDemo());
  runApp(HandAccountDemo());
  // runApp(TabBarDemo());
  // runApp(TextFieldDemo01());
  // runApp(TextFieldDemo2());
  // runApp(FormDemo());
  // runApp(DropdownButtonDemo());
  // runApp(UpdateDataOverTheInternetDemo());
}


